% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gibbs-mismatch.R
\name{gibbs_mismatch}
\alias{gibbs_mismatch}
\title{Gibbs Sampler for Mismatch Model}
\usage{
gibbs_mismatch(
  y,
  sigma,
  X,
  init = init_mismatch(m = length(y), d = ncol(X)),
  control = control_mismatch(),
  fixed = fixed_mismatch()
)
}
\arguments{
\item{y}{Observed point estimates.}

\item{sigma}{Fixed variances.}

\item{X}{Design matrix for regression on point estimates.}

\item{init}{Initial values from \link{init_mismatch}.}

\item{control}{Control object from \link{control_mismatch}.}

\item{fixed}{Fixed value indicators from \link{fixed_mismatch}.}
}
\value{
A list with results from the sampler.
}
\description{
Run the Gibbs sampler.
}
\examples{
\dontrun{
# Simulate data
set.seed(1234)

m = 500
tau_true = sqrt(0.25)
phi_true = sqrt(0.2)
beta_true = c(1.5, 0.85)
gamma_true = c(2.6, -1)
df = rchisq(m, 16)
X = cbind(1, rnorm(m, 8, 2))
Z = cbind(1, rnorm(m, 7, 1.25))

sigma2_true = rlnorm(m, Z \%*\% gamma_true, tau_true)
theta_true = rnorm(m, X \%*\% beta_true, phi_true)
s2 = sigma2_true / df * rchisq(m, df)
y = rnorm(m, theta_true, sqrt(s2))

ctrl = control_joint(R = 100, report = 20)
gibbs_out = gibbs_joint(y, s2, X, Z, df, control = ctrl)
}

}
