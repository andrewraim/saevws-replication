% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gibbs-joint.R
\name{gibbs_joint}
\alias{gibbs_joint}
\title{Gibbs Sampler for Joint SAE Model}
\usage{
gibbs_joint(
  y,
  s2,
  X,
  Z,
  df,
  init = init_joint(m = length(y), d1 = ncol(X), d2 = ncol(Z)),
  control = control_joint(),
  fixed = fixed_joint()
)
}
\arguments{
\item{y}{Observed point estimates.}

\item{s2}{Observed variance estimates.}

\item{X}{Design matrix for regression on point estimates.}

\item{Z}{Design matrix for regression on variance estimates.}

\item{df}{Degrees of freedom to use for variance estimates.}

\item{init}{Initial values from \link{init_joint}.}

\item{control}{Control object from \link{control_joint}.}

\item{fixed}{Fixed value indicators from \link{fixed_joint}.}
}
\value{
A list with results from the sampler.
}
\description{
Run the Gibbs sampler.
}
\examples{
\dontrun{
set.seed(1234)

# Simulate data
m = 500
tau_true = sqrt(0.25)
phi_true = sqrt(0.2)
beta_true = c(1.5, 0.85)
gamma_true = c(2.6, -1)
df = rchisq(m, 16)
X = cbind(1, rnorm(m, 8, 2))
Z = cbind(1, rnorm(m, 7, 1.25))

sigma2_true = rlnorm(m, Z \%*\% gamma_true, tau_true)
theta_true = rnorm(m, X \%*\% beta_true, phi_true)
s2 = sigma2_true / df * rchisq(m, df)
y = rnorm(m, theta_true, sqrt(s2))

inner = control_inner(tol_suff = 0.90, tol_merge = 0.01, tune = 50)
ctrl = control_joint(R = 100, report = 20, inner = inner)
out = gibbs_joint(y, s2, X, Z, df, control = ctrl)
plot(out$tau2[,1], type = "l")
}

}
