% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proposal-plots.R
\name{where_leveled}
\alias{where_leveled}
\title{Where Leveled}
\usage{
where_leveled(x, burn, tol, increasing = TRUE)
}
\arguments{
\item{x}{Numeric vector containing the series of interest.}

\item{burn}{Integer scalar; burn-in period before considering the series to
have leveled off.}

\item{tol}{Numeric scalar that determines how much of the initial dramatic}

\item{increasing}{Logical scalar; \code{TRUE} if the series is expected to
increase and then level off, \code{FALSE} to instead handle series which are
expected to decrease and then level off.}
}
\value{
An integer
}
\description{
A utility to plot a series which increases dramatically at the beginning and
then tends to level off but still contain variability. The objective is to
find an index where the dramatic increase is beginning to level off. The
caller can use this to zoom into the leveled-off data. Similarly, it can be
used for series which decrease.
}
\details{
Suppose the series \code{x} is expected to increase dramatically and then level
off (the caller should confirm this). Let \code{x0} be the minimum value of \code{x}
that occurs after index \code{burn}. Return the first index of \code{x} whose value is
larger than \verb{(1 - tol) * x0)}.

Now suppose \code{x} expected to decrease dramatically and then level off. Let
\code{x0} be the maximum value of \code{x} that occurs after index \code{burn}. Return the
first index of \code{x} whose value is smaller than \verb{(1 + tol) * x0)}.
}
