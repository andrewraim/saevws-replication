% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gibbs-unmatch.R
\name{gibbs_unmatch}
\alias{gibbs_unmatch}
\title{Gibbs Sampler for Unmatched SAE Model}
\usage{
gibbs_unmatch(
  y,
  sigma,
  X,
  init = init_unmatch(m = length(y), d = ncol(X)),
  control = control_unmatch(),
  fixed = fixed_unmatch()
)
}
\arguments{
\item{y}{Observed point estimates.}

\item{sigma}{Fixed variances.}

\item{X}{Design matrix for regression on point estimates.}

\item{init}{Initial values from \link{init_unmatch}.}

\item{control}{Control object from \link{control_unmatch}.}

\item{fixed}{Fixed value indicators from \link{fixed_unmatch}.}
}
\value{
A list with results from the sampler.
}
\description{
Run the Gibbs sampler.
}
\examples{
\dontrun{
set.seed(1234)

# Simulate data
m = 500
X = cbind(1, rnorm(m))
sigma2 = rgamma(m, 1.25, 1/20)
sigma = sqrt(sigma2)

beta_true = c(1, -1)
Xbeta_true = X \%*\% beta_true
tau_true = 1.25
mu_true = rlnorm(m, Xbeta_true, tau_true)
y = rnorm(m, mu_true, sigma)

inner = control_inner(tol_suff = 0.90, tol_merge = 0.01, tune = 50)
ctrl = control_unmatch(R = 100, report = 20)
out = gibbs_unmatch(y, sigma, X, control = ctrl)
plot(out$tau2, type = "l")
}

}
